name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # Required for SonarCloud analysis

    # Backend setup and build
    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven

    - name: Build Backend
      run: |
        cd back
        mvn clean install

    - name: Run Backend Tests with Coverage
      run: |
        cd back
        mvn clean test jacoco:report

    # Frontend setup and build
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'
        cache: 'npm'
        cache-dependency-path: 'front/package-lock.json'

    - name: Install Frontend Dependencies
      run: |
        cd front
        npm ci

    - name: Build Frontend
      run: |
        cd front
        npm run build

    - name: Run Frontend Tests
      run: |
        cd front
        npm run test -- --watch=false --code-coverage

    # SonarCloud Analysis (Backend + Frontend)
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      with:
        args: >
          -Dsonar.projectKey=Gautier-DC_P10_projet_collaboratif_CI-CD
          -Dsonar.organization=gautier-dc
          -Dsonar.sources=front/src,back/src/main
          -Dsonar.tests=back/src/test,front/src
          -Dsonar.test.inclusions=**/*.spec.ts,**/src/test/**
          -Dsonar.exclusions=**/node_modules/**,**/*.spec.ts,**/target/**
          -Dsonar.typescript.lcov.reportPaths=front/coverage/lcov.info
          -Dsonar.coverage.jacoco.xmlReportPaths=back/target/site/jacoco/jacoco.xml
          -Dsonar.java.binaries=back/target/classes
